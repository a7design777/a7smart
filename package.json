{
  "name": "a7smart",
  "version": "0.1.0",
  "private": true,
  "description": "Дашборд керування квартирами на Tuya Cloud — smart.zvyazok.com",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server & npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace web && npm run build --workspace server",
    "start": "node server/dist/index.js",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "lint": "eslint .",
    "db:migrate": "npm run migrate --workspace server",
    "tuya:probe": "npm run probe --workspace server",
    "status": "npm run status --workspace server",
    "remihome:probe": "npm run probe:remihome --workspace server"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
